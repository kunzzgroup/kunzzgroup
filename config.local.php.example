<?php
/**
 * Local XAMPP overrides — copy to config.local.php (gitignored).
 * Required when the project lives under http://localhost/kunzzgroup/
 */
$dbname = 'kunzz';
$dbuser = 'root';
$dbpass = '';

// Uncomment if app_url() does not auto-detect the subdirectory:
// $appBasePath = 'kunzzgroup';
