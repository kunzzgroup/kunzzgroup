# 修复记录：批量保存报错 + 旧记录被莫名删除

> 日期：2026-08-23
> 修改文件：5 个（见各问题下方）
> 状态：代码已改完，**尚未部署到线上**，需先备份数据库再部署测试

---

## 问题一：批量保存超过 10 个货品报错

### 现象

在 `stockeditall?system=j3`（J1/J2/Central 同样受影响）批量保存 10 个以上的货品时，页面弹出：

```
数据库连接失败：SQLSTATE[HY000] [2002] Operation not permitted
```

10 个以内正常，超过 10 个就失败。

### 根因

**不是数据库坏了，也不是代码逻辑错误**，而是"请求数量太多"触发了 Hostinger 共享主机的**并发进程/连接数限制**。

点击一次"批量保存"，前端原来会连续发起 **2N+1 个请求**：

1. 每个出货货品查一次备注码（约 N 个请求）
2. 每个出库产品查一次库存（约 N 个请求）
3. 最后 1 个保存请求

保存 10 个货品 = 20+ 个请求瞬间叠加，服务器连接数被顶爆，轮到保存请求时连数据库都连不上。

另外发现：J1/J2/J3 的后端**根本没有"备注码查询"接口**，那 N 个备注码请求全是"白发的空请求"，从来没起到过校验作用。

### 修复方式（3 步）

| 步骤 | 修改 | 文件 |
|------|------|------|
| 1 | **移除前端库存预检查**（省 N 个请求）——库存校验由后端在事务内完成，逻辑一致且更权威 | `backend/js/stockeditall.js` |
| 2 | **备注码检查只对 Central 执行 + 按产品去重**（省 N 个请求）——J1/J2/J3 后端无此接口，跳过无用请求；Central 保留校验且同一产品只查一次 | `backend/js/stockeditall.js` |
| 3 | **数据库连接失败自动重试 3 次**（0.3s/0.6s 退避）——应对偶发的资源限制波动，只重试"建立连接"，绝不重试写入，不会重复插入 | `backend/stockeditapi.php`、`backend/j1stockeditpageapi.php`、`backend/j2stockeditpageapi.php`、`backend/j3stockeditpageapi.php` |

### 修复后效果

- J3 批量保存：**20+ 个请求 → 1 个请求**（无论保存几个货品）
- Central 批量保存：**2N+1 个请求 → 1~2 个请求**
- 服务器负载大幅降低，其他用户同时使用也更稳定

---

## 问题二：旧记录被莫名删除（数据丢失！）

### 现象

用户调查库存时，发现某些历史记录（几个月前的）莫名其妙消失了，且**回收站里也找不到**。

### 根因

`backend/stockeditapi.php` 的**编辑功能（handlePut）**里有 9 处**危险的硬删除语句**：

```php
-- 修改前（危险）：
DELETE FROM j3stockedit_data
WHERE product_name = ? AND receiver = ? AND target_system = 'j3'
```

这条语句**没有任何日期/ID 限制**，只按"产品名 + 收货人 + 系统"匹配。

**触发场景**（在 Central 页面编辑一条出库记录时）：

1. 把货品从 A 改成 B
2. 改了目标系统（如 J3 → J1）
3. 把目标系统改为 Central

→ 后端执行上述删除 → **J3 表里所有 "A产品 + 该收货人" 的历史记录被全部硬删**（包括几个月前的旧记录，且硬删除不可恢复）。

### 修复方式

9 处全部改为**按 main_record_id 精确删除**：

```php
-- 修改后（安全）：
DELETE FROM j3stockedit_data
WHERE main_record_id = ? AND target_system = 'j3'
```

`main_record_id` 是每条 J 系统记录在同步时挂上的"中央记录 ID"，用它删除**只会删掉当前编辑这条记录的同步副本，历史记录完全不动**。

### 修复后效果

- 编辑记录（换货品/换系统）只影响当前记录本身
- 历史记录永不误删
- J1/J2/J3/Central 四个系统全部生效

---

## ⚠️ 部署与测试建议

1. **先备份数据库**（Hostinger hPanel → 备份，确认有可恢复点）
2. 上传修改的 5 个文件到服务器
3. 测试：
   - **批量保存**：J3 页面添加 15~20 个货品，点批量保存 → 应一次成功，无报错
   - **库存不足**：故意让某货品出库数量超过库存 → 应提示"库存不足"且整批不保存（由后端校验，行为与之前一致）
   - **编辑不误删**：Central 页面编辑一条出库记录、换货品名 → 保存后检查 J 系统对应历史记录是否还在
   - **备注码校验**：Central 页面出货带备注码的货品，不填备注编号 → 应被拦截（校验保留）
4. 确认无误后，再 push 到 GitHub 同步代码

## 已知边界（不影响主数据）

通过"Central 编辑流程"创建的极少数同步记录可能没有挂 main_record_id，这类记录在换货品/换系统时同步副本可能残留一条冗余行（不会误删历史）。后续可再优化。
